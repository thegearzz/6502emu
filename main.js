function parseOpCode(opCode){
	switch(opCode){
		case ADC:
			//add memory to accumulator and carry
			break:
		case AND:
			//AND memort to accumulator
			break:
		case ASL:
			//shift left one bit (memory or accumulator)
			break:
		case BCC:
			//branch on carry clear
			break:
		case BCS:
			//branch on carry set
			break:
		case BEQ:
			//branch if result zero
		}
	}
}
